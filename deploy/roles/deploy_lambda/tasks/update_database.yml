---
- name: Update database
  shell: |
    bash -c "source venv/bin/activate && python formatted.py && export ENV=DEPLOY && uvicorn app.main:app --host 0.0.0.0 --port 8000 & sleep 10 && pkill uvicorn"
  args:
    chdir: "../app/backend"
